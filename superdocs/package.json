{
  "name": "superdocs",
  "displayName": "superdocs",
  "description": "Agentic programming assistant",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.82.3"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "views": {
      "explorer": [
        {
          "type": "webview",
          "id": "superdocs.superdocsView",
          "name": "Superdocs"
        }
      ]
    },
    "commands": [
      {
        "command": "superdocs.addSnippet",
        "title": "Superdocs: Add Snippet"
      },
      {
        "command": "superdocs.addTerminal",
        "title": "Superdocs: Add Terminal Content"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "superdocs.addSnippet",
          "when": "editorHasSelection"
        },
        {
          "command": "superdocs.addTerminal"
        }
      ]
    },
    "configuration": {
      "title": "Superdocs",
      "properties": {
        "superdocs.openAiApiKey": {
          "type": "string",
          "default": "",
          "description": "The OpenAI Key you plan to use. You should restrict how much this key can spend as much as you can."
        },
        "superdocs.pythonServerPath": {
          "type": "string",
          "default": "",
          "description": "Absolute path to your Python server"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p src",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/express": "^4.17.20",
    "@types/glob": "^8.1.0",
    "@types/mocha": "^10.0.1",
    "@types/node": "20.2.5",
    "@types/vscode": "^1.83.0",
    "@typescript-eslint/eslint-plugin": "^5.59.8",
    "@typescript-eslint/parser": "^5.59.8",
    "@vscode/test-electron": "^2.3.2",
    "eslint": "^8.41.0",
    "glob": "^8.1.0",
    "mocha": "^10.2.0",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "node-fetch": "^3.3.2"
  }
}
